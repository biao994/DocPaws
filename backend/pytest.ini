[pytest]
testpaths = tests
markers =
    integration: live API connectivity tests (require .env keys and RUN_INTEGRATION=1)
    pg: PostgreSQL integration tests (require docker postgres and RUN_PG_INTEGRATION=1)
addopts = -m "not integration and not pg"
