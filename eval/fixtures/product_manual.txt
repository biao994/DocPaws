DocPaws 产品手册（企业版）

DocPaws 企业版提供知识库管理、PDF 增量索引、FAISS 向量检索与 SSE 流式对话。

上传：仅支持 PDF；支持文件夹批量上传与增量重建索引。

对话：支持 fast 快速模式与 deep 深度思考模式；回答可带 chunk 引用与页码。

安全：会话采用 Cookie Session；知识库按用户隔离。索引任务失败时文档状态标记为 failed。

部署：后端 FastAPI + SQLite，向量索引目录可配置 INDEX_DIR。
